pipeline {
agent any
stages {
        stage('Build') {
            steps {
                echo 'Build is success'
				
            }
        }
        stage('deploy') {
            steps {
                echo 'Deploying to TEST environment..'
            }
        }
        stage('test') {
            steps {
                echo 'Testing....'
					}
				}
    }
}
